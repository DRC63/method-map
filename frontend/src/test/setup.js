// Vitest global setup (referenced from vite.config.js `test.setupFiles`). Loading
// jest-dom here registers its DOM matchers (toBeInTheDocument, toHaveTextContent,
// …) for every test file, so individual tests don't each have to import it.
import '@testing-library/jest-dom';
